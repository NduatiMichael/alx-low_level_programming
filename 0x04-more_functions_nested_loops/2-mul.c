#include "main.h"
/**
 * mul - Function to multiply two numbers
 * @a: first digit
 * @b: second digit
 * Return: returns the results
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
