#ifndef HEADER_FILE
#define HEADER_FILE
#include "main.h"
/**
 *_abs - Prints the absolute value of an input
 *
 *@int: the number to check
 * Return: absolute
 */
int _abs(int)
{
	int absolute;

	if (int > 0)
	{
		absolute = int;
	}
	else if (n == 0)
	{
		absolute = 0;
	}
	else
	{
		absolute = int * -1;
	}
	return (absolute);
}
#endif
